package Test_DAO;

import DAO.UserDAO;
import domain.User;
import org.dom4j.DocumentException;
import org.junit.Test;

import java.io.IOException;

/*
* 这是UserDAO的测试类/
* 测试类的特点：测试方法的结构：无返回值，没有参数
* */
public class TestUserDao {
    /*@Test
    public void testFindByUserID() throws DocumentException {
        UserDAO userDAO = new UserDAO();
        User user = userDAO.findByUserID("123456718");
        System.out.println(user);
    }
    @Test
    public void testAdd() throws IOException, DocumentException {
        User user = new User();
        //添加新用户
        user.setUserName("张三三");
        user.setEmail("1234");
        user.setPassWord("1234");
        user.setPhone("1234");
        user.setID("123456718");

        UserDAO userDAO = new UserDAO();
        userDAO.add(user);
    }*/
}
